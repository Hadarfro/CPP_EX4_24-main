#include "node.hpp"
#include <iostream>
#include <vector>

using namespace std;

// template<typename T,int N = 2>
// // Constructor to initialize the node with a value
// explicit Node::Node(T val, vector<Node> k = {}) : value(val), children(k), maxChild(N) {
// // children.resize(2);
// }

// T Node::get_value(){
//     return value;
// }